#include <stdio.h>
#include <stdlib.h>
#include "bhargav.h"

int main()
{
    // int *ary, size;
    // size = 5;
    // ary = int_Memo(size);
    // ary[4] = 1;
    // printf("%d\n", ary[4]);
}